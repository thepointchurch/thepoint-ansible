[tool.poetry]
name = "thepoint-ansible"
version = "2.4.0"
description = ""
authors = ["Craig Holyoak <craig@helmsdeep.org>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
mitogen = "^0.3.7"
ansible = "^9.4"
ansible-lint = { version = "^24.2", markers = "platform_system != 'Windows'" }
jinja2 = "^3.1"
pyyaml = "^6.0"
boto = "^2.49"
boto3 = "^1.34"
cryptography = "^42.0"

[tool.poetry.dev-dependencies]
pre-commit = "^3.7"
