[tool.poetry]
name = "thepoint-ansible"
version = "2.4.0"
description = ""
authors = ["Craig Holyoak <craig@helmsdeep.org>"]

[tool.poetry.dependencies]
python = "^3.9"
mitogen = "^0.3.4"
ansible = "^8.1"
ansible-lint = { version = "^6.13", markers = "platform_system != 'Windows'" }
jinja2 = "^3.0"
pyyaml = "^6.0"
boto = "^2.49"
boto3 = "^1.20"
cryptography = "^41.0"

[tool.poetry.dev-dependencies]
pre-commit = "^3.0"
